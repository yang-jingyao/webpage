# Database Configuration
# 在 Vercel 中设置此环境变量为您的 Vercel Postgres 连接字符串
DATABASE_URL=postgresql://username:password@host:port/database

# 本地开发时，您可以使用以下格式：
# DATABASE_URL=postgresql://localhost:5432/flosendo_waitlist

# 生产环境示例（Vercel Postgres）：
# DATABASE_URL=postgres://default:password@ep-xxx.us-east-1.postgres.vercel-storage.com/verceldb?sslmode=require

# Node Environment
NODE_ENV=development
